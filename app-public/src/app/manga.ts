import { Component, OnInit } from "@angular/core";

export class Manga {
    _id: string;
    title: string;
    author: string;
    description: string;
    price: number;
    publication_year: Date;
}
